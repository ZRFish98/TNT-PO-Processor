# Streamlit Secrets Configuration
# Copy this file to secrets.toml and fill in real values
# NEVER commit secrets.toml to git!

[supabase]
url = "https://wfgxdqblcoxzspbrwpmh.supabase.co"
# For production, use anon_key (public key) - safe for client-side operations
anon_key = "eyJhbGc...your-anon-key-here"
# For development/admin operations only, use service_key
service_key = "sbp_...your-service-key-here"

[odoo]
url = "https://atiara-trading-inc.odoo.com"
db = "atiara-trading-inc"
api_key = "your-odoo-api-key-here"
default_user = "official@atiara.ca"

# For Streamlit Cloud Deployment:
# 1. Go to your app settings
# 2. Navigate to "Secrets" section
# 3. Copy the contents above (with real values) into the secrets editor
# 4. Click "Save"

# Security Notes:
# - Use anon_key for client-side operations with RLS enabled
# - Use service_key ONLY for admin operations and server-side code
# - Never expose service_key in client-side code or logs
# - Keep this file in .gitignore
